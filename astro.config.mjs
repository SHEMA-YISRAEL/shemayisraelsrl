// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://SHEMA-YISRAEL.github.io', // username github
  base: 'shemayisraelsrl', // name repo
});
